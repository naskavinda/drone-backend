spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/drone
    username: root
    password: password
  sql:
    init:
      mode: always
logging:
  level:
    org:
      hibernate:
        type: trace
